openapi: 3.2.0
info:
  title: OAI Service Delete Record API
  version: 0.0.1
servers:
- url: /api/v1
security:
- apiKey: []
tags:
- name: Delete Record
paths:
  /delete-record:
    post:
      summary: Delete a record
      operationId: deleteRecord
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeleteRecordRequest'
        required: true
      responses:
        '200':
          description: Success
        default:
          $ref: '#/components/responses/Error'
      tags:
      - Delete Record
components:
  schemas:
    DeleteRecordRequest:
      type: object
      properties:
        identifier:
          type: string
          minLength: 1
        metadata_prefix:
          type: string
          minLength: 1
      required:
      - identifier
      - metadata_prefix
    Error:
      type: object
      properties:
        code:
          type: integer
          format: int64
        message:
          type: string
      required:
      - code
      - message
  responses:
    Error:
      description: Error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
  securitySchemes:
    apiKey:
      type: apiKey
      in: header
      name: X-Api-Key
