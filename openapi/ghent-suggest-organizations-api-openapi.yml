openapi: 3.2.0
info:
  title: People Service Suggest Organizations API
  version: 0.0.1
servers:
- url: /api/v1
security:
- apiKey: []
tags:
- name: Suggest Organizations
paths:
  /suggest-organizations:
    post:
      summary: Search on organization records
      operationId: SuggestOrganizations
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SuggestOrganizationsRequest'
        required: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationListResponse'
        default:
          $ref: '#/components/responses/Error'
      tags:
      - Suggest Organizations
components:
  schemas:
    Organization:
      type: object
      properties:
        id:
          type: string
        date_created:
          type: string
          format: date-time
        date_updated:
          type: string
          format: date-time
        type:
          type: string
        acronym:
          type: string
        name_dut:
          type: string
        name_eng:
          type: string
        parent:
          type: array
          items:
            $ref: '#/components/schemas/OrganizationParent'
        identifier:
          type: array
          items:
            type: string
    Error:
      type: object
      properties:
        code:
          type: integer
          format: int64
        message:
          type: string
      required:
      - code
      - message
    OrganizationParent:
      type: object
      properties:
        id:
          type: string
        date_created:
          type: string
          format: date-time
        date_updated:
          type: string
          format: date-time
        from:
          type: string
          format: date-time
        until:
          type: string
          format: date-time
      required:
      - id
      - from
    OrganizationListResponse:
      type: object
      required:
      - data
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Organization'
    SuggestOrganizationsRequest:
      type: object
      properties:
        limit:
          type: integer
          minimum: 0
          maximum: 100
        query:
          type: string
          minLength: 1
      required:
      - query
  responses:
    Error:
      description: Error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
  securitySchemes:
    apiKey:
      type: apiKey
      in: header
      name: X-Api-Key
