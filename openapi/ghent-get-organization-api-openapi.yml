openapi: 3.2.0
info:
  title: People Service Get Organization API
  version: 0.0.1
servers:
- url: /api/v1
security:
- apiKey: []
tags:
- name: Get Organization
paths:
  /get-organization:
    post:
      summary: Get single organization record
      operationId: GetOrganization
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetOrganizationRequest'
        required: true
      responses:
        '200':
          description: Retrieve a single organization record successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Organization'
        default:
          $ref: '#/components/responses/Error'
      tags:
      - Get Organization
components:
  schemas:
    Organization:
      type: object
      properties:
        id:
          type: string
        date_created:
          type: string
          format: date-time
        date_updated:
          type: string
          format: date-time
        type:
          type: string
        acronym:
          type: string
        name_dut:
          type: string
        name_eng:
          type: string
        parent:
          type: array
          items:
            $ref: '#/components/schemas/OrganizationParent'
        identifier:
          type: array
          items:
            type: string
    Error:
      type: object
      properties:
        code:
          type: integer
          format: int64
        message:
          type: string
      required:
      - code
      - message
    OrganizationParent:
      type: object
      properties:
        id:
          type: string
        date_created:
          type: string
          format: date-time
        date_updated:
          type: string
          format: date-time
        from:
          type: string
          format: date-time
        until:
          type: string
          format: date-time
      required:
      - id
      - from
    GetOrganizationRequest:
      type: object
      properties:
        id:
          type: string
          minLength: 1
      required:
      - id
  responses:
    Error:
      description: Error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
  securitySchemes:
    apiKey:
      type: apiKey
      in: header
      name: X-Api-Key
