openapi: 3.2.0
info:
  title: Projects Service Suggest Projects API
  version: 0.0.1
servers:
- url: /api/v1
security:
- apiKey: []
tags:
- name: Suggest Projects
paths:
  /suggest-projects:
    post:
      summary: Search in projects
      operationId: suggestProjects
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SuggestProjectsRequest'
        required: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuggestProjectsResponse'
        default:
          $ref: '#/components/responses/Error'
      tags:
      - Suggest Projects
components:
  schemas:
    SuggestProjectsRequest:
      type: object
      properties:
        query:
          type: string
          minLength: 0
      required:
      - query
    SuggestProjectsResponse:
      type: object
      properties:
        cursor:
          type: string
        data:
          type: array
          items:
            $ref: '#/components/schemas/GetProject'
      required:
      - data
    Project:
      type: object
      properties:
        id:
          type: string
        created:
          type: string
          format: date-time
        modified:
          type: string
          format: date-time
        type:
          type: string
        identifier:
          type: array
          items:
            type: object
            properties:
              type:
                type: string
              propertyID:
                type: string
              value:
                type: string
            required:
            - type
            - propertyID
            - value
        isFundedBy:
          type: object
          properties:
            type:
              type: string
            hasCallNumber:
              type: string
            isAwardedBy:
              type: object
              nullable: true
              properties:
                type:
                  type: string
                name:
                  type: string
              required:
              - type
              - name
          required:
          - type
        hasAcronym:
          type: array
          items:
            type: string
        name:
          type: array
          items:
            type: object
            properties:
              language:
                type: string
              value:
                type: string
            required:
            - language
            - value
        description:
          type: array
          items:
            type: object
            properties:
              language:
                type: string
              value:
                type: string
            required:
            - language
            - value
        foundingDate:
          type: string
        dissolutionDate:
          type: string
    GetProject:
      allOf:
      - $ref: '#/components/schemas/Project'
      - required:
        - id
        - type
        - identifier
        - created
        - modified
    Error:
      type: object
      properties:
        code:
          type: integer
          format: int64
        message:
          type: string
      required:
      - code
      - message
  responses:
    Error:
      description: Error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
  securitySchemes:
    apiKey:
      type: apiKey
      in: header
      name: X-Api-Key
